<!-- Alertas -->

<% if (errors.length > 0) { %>
<div class="alert alert-danger fade-in">
  <% errors.forEach(erro => { %>
    <%= erro %> <br>
  <% }) %>
</div>
<% } %>

<% if(success.length > 0) { %>
<div class="alert alert-sucess fade-in">
  <% success.forEach(success => { %>
    <%= success %> <br>
  <% }) %>
</div>
<% } %>
